{
  "name": "mmscheduler-scraper",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "auth": "node auth.mjs",
    "fetch": "node fetch.mjs",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p 'test_*.py' -v"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "1.62.1"
  }
}
